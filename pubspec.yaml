name: project_circuit_flutter
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  provider: ^6.1.2
  equatable: ^2.0.5
  get: ^4.6.6
  flutter_secure_storage: ^9.2.2
  localstorage: ^5.0.0
  carousel_slider: ^5.0.0
  intl: ^0.19.0
  flutter_launcher_icons: ^0.14.1
  mockito: ^5.4.4 # Gerar versões mockadas de classes para testes
  test: ^1.25.7 # Rodar os testes apenas com o Dart, sem precisar do Flutter
  faker: ^2.2.0 # Gerar dados falsos para testes

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
